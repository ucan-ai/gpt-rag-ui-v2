# 🔧 REQUIRED CONFIGURATION

# URL of the orchestrator streaming endpoint (usually your Function App's stream API URL)
ORCHESTRATOR_STREAM_ENDPOINT=https://<your-function-app>.azurewebsites.net/api/orcstream

# Azure Subscription ID used for function key retrieval
AZURE_SUBSCRIPTION_ID=<your-subscription-id>

# Resource group where the orchestrator Function App is deployed
AZURE_RESOURCE_GROUP_NAME=<your-resource-group-name>

# Name of the orchestrator Function App
AZURE_ORCHESTRATOR_FUNC_NAME=<your-orchestrator-function-app-name>

# Azure Blob Storage account name containing your documents
BLOB_STORAGE_ACCOUNT_NAME=<your-storage-account-name>

# Name of the container in your blob storage account
BLOB_STORAGE_CONTAINER=<your-container-name>

# 🔐 SESSION CONFIGURATION

# Secret key for Chainlit sessions (use a secure random string)
CHAINLIT_SECRET_KEY=supersecret

# 🔑 OPTIONAL – Entra ID Authentication (Enable only if needed)
# Set ENABLE_AUTHENTICATION=true to enable auth
ENABLE_AUTHENTICATION=false

# Application (Client) ID registered in Entra ID
CLIENT_ID=<your-client-id>

# Authority URL (usually https://login.microsoftonline.com/<tenant-id>)
AUTHORITY=https://login.microsoftonline.com/<your-tenant-id>

# Secret of the App registration
APP_SERVICE_CLIENT_SECRET=<your-client-secret>

# Redirect path for OAuth flow (usually keep default)
REDIRECT_PATH=/getAToken
